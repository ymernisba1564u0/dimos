[supervisord]
nodaemon=true
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisord.pid

[program:ros2]
command=/bin/bash -c "source /opt/ros/humble/setup.bash && source /ros2_ws/install/setup.bash && ros2 launch go2_robot_sdk robot.launch.py"
autostart=true
autorestart=true

stderr_logfile=/app/assets/output/ros2.err.log
stdout_logfile=/app/assets/output/ros2.out.log
environment=PYTHONUNBUFFERED=1,PYTHONPATH=/app:%(ENV_PYTHONPATH)s,ROBOT_IP=%(ENV_ROBOT_IP)s,CONN_TYPE=%(ENV_CONN_TYPE)s,WEBRTC_SERVER_HOST=%(ENV_WEBRTC_SERVER_HOST)s,WEBRTC_SERVER_PORT=%(ENV_WEBRTC_SERVER_PORT)s

[program:dimos]
command=/bin/bash -c "sleep 10 && source /opt/ros/humble/setup.bash && source /ros2_ws/install/setup.bash && python3 /app/dimos/robot/unitree/run_go2_ros.py"
autostart=true
autorestart=true
startsecs=11

stderr_logfile=/app/assets/output/dimos.err.log
stdout_logfile=/app/assets/output/dimos.out.log
environment=PYTHONUNBUFFERED=1,PYTHONPATH=/app:%(ENV_PYTHONPATH)s,ROBOT_IP=%(ENV_ROBOT_IP)s,CONN_TYPE=%(ENV_CONN_TYPE)s,WEBRTC_SERVER_HOST=%(ENV_WEBRTC_SERVER_HOST)s,WEBRTC_SERVER_PORT=%(ENV_WEBRTC_SERVER_PORT)s

[unix_http_server]
file=/var/run/supervisor.sock
chmod=0700

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock